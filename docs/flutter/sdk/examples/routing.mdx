---
sidebar: fluttersdk
---
# Маршруты

В этом разделе описывается работа с маршрутами для разных видов транспорта. Вы можете создавать маршруты двумя способами:

* [Построить новый маршрут](#building-route) по компонентам:

    * Точки маршрута: начальная, конечная и, если необходимо, промежуточные.
    * [Параметры поиска маршрута](#route-parameters). Набор параметров зависит от типа транспорта, для которого строится маршрут.

* [Использовать готовый маршрут](#using-ready-route), который был построен ранее с помощью [Routing API](/api/navigation/routing/overview). Готовый маршрут уже содержит необходимые данные для отображения и навигации.

## Построение маршрута {#building-route}

Для работы с маршрутом вам понадобится два объекта:

* [TrafficRouter](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/TrafficRouter-class.html) для создания или загрузки маршрута;
* источник данных [RouteMapObjectSource](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteMapObjectSource-class.html) для отображения маршрута на карте.

1. Чтобы создать новый маршрут, вызовите метод [findRoute()](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/TrafficRouter/findRoute.html), передав координаты точек в виде объектов [RouteSearchPoint](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteSearchPoint-class.html). Дополнительно можно указать параметры маршрута ([RouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteSearchOptions-class.html)), а также список промежуточных точек маршрута (список [RouteSearchPoint](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteSearchPoint-class.html)).

    ```dart
    import 'package:dgis_mobile_sdk_full/dgis.dart' as sdk;

    final routeSearchOptions = sdk.RouteSearchOptions.car(sdk.CarRouteSearchOptions());
    final startPoint = sdk.RouteSearchPoint(coordinates: GeoPoint(latitude: 55.759909, longitude: 37.618806));
    final finishPoint = sdk.RouteSearchPoint(coordinates: GeoPoint(latitude: 55.752425, longitude: 37.613983));

    final trafficRouter = sdk.TrafficRouter(sdkContext);
    final routesFuture = trafficRouter.findRoute(startPoint, finishPoint, routeSearchOptions);
    ```

    Вызов `findRoute()` вернёт отложенный результат со списком объектов [TrafficRoute](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/TrafficRoute-class.html).

2. Чтобы отобразить найденный маршрут на карте, создайте объекты [RouteMapObject](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteMapObject-class.html) на основе полученных объектов `TrafficRoute` и добавьте их в источник данных [RouteMapObjectSource](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteMapObjectSource-class.html).

    ```dart
    import 'package:dgis_mobile_sdk_full/dgis.dart' as sdk;

    // Создаём источник данных
    final sdk.RouteMapObjectSource routeMapObjectSource = sdk.RouteMapObjectSource(sdkContext, RouteVisualizationType.NORMAL)
    map.addSource(routeMapObjectSource)

    // Добавляем созданный маршрут на карту
    final List<sdk.TrafficRoute> routes = await trafficRouter.findRoute(startSearchPoint, finishSearchPoint).value;

    int routeIdx = 0;
    bool isActive = true;
    routes.forEach((sdk.TrafficRoute route) {
      routeMapObjectSource.addObject(sdk.RouteMapObject(route, isActive, routeIdx));
      routeIdx += 1;
      isActive = false;
    })
    ```

Вместо пары [TrafficRouter](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/TrafficRouter-class.html) и [RouteMapObjectSource](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteMapObjectSource-class.html) для построения маршрута можно использовать [RouteEditor](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteEditor-class.html) и [RouteEditorSource](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteEditorSource-class.html). В таком случае не нужно обрабатывать список [TrafficRoute](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/TrafficRoute-class.html), достаточно передать координаты маршрута в виде объекта [RouteEditorRouteParams](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteEditorRouteParams-class.html) в метод [setRouteParams()](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteEditor/setRouteParams.html), и маршрут отобразится автоматически.

```dart
final routeEditor = sdk.RouteEditor(sdkContext);
final routeEditorSource = sdk.RouteEditorSource(sdkContext, routeEditor);
map.addSource(routeEditorSource);

routeEditor.setRouteParams(
  sdk.RouteEditorRouteParams(
    startPoint: startPoint,
    finishPoint: finishPoint,
    routeSearchOptions: routeSearchOptions,
  ),
);
```

## Точки маршрута {#getting-route-points}

Для построения маршрута необходимо получить точки типа [GeoPoint](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/GeoPoint-class.html), которые далее используются для создания [RouteSearchPoint](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteSearchPoint-class.html). Существует множество способов это сделать, ниже описан один из них.

### Получение координат объекта в месте нажатия на карту {#getting-coordinates-on-tap}

<img src="/img/mobile-sdk/device-2022-08-22-122901.gif"  />

Можно получить координаты объекта (здания, дороги), который находится в месте клика. Для этого понадобится метод [getRenderedObjects()](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/Map/getRenderedObjects.html). В нем нужно получить объект класса [DgisMapObject](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/DgisMapObject-class.html), который имеет поле `id`. Этот идентификатор является стабильным числовым идентификатором объекта справочника, с помощью которого можно выполнить поисковый запрос и получить информацию об объекте, в том числе географические координаты его центра.

```dart
import 'package:dgis_mobile_sdk_map/dgis.dart' as sdk;

map.getRenderedObjects(sdk.ScreenPoint()).then((List<sdk.RenderedObjectInfo> info) {
  // Получим ближайший к месту нажатия объект внутри установленного радиуса
  final sdk.RenderedObject obj = info.first.item;

  // В этом примере мы хотим найти информацию о выбранном объекте в справочнике.
  // Для этого мы должны убедиться, что тип этого объекта может быть найден
  if (obj.source is sdk.DgisSource && obj.item is sdk.DgisMapObject) {
    final source = obj.source as sdk.DgisSource;

    // Произведем поиск
    final foundObject =
        await sdk.SearchManager.createOnlineManager(sdkContext)
            .searchByDirectoryObjectId((obj.item as sdk.DgisMapObject).id)
            .value;

    final sdk.GeoPoint? geoPoint = foundObject.markerPosition?.point;
  }
});
```

:::note[Координаты больших объектов]

Координаты центра для больших объектов (например, МКАД) могут находиться далеко за пределами области видимости камеры, так как полученный объект не учитывает расположение камеры или места нажатия на карту. С такими объектами лучше использовать другие способы получения координат.

:::

### Проезд промежуточных точек {#passing-intermediate-points}

Для маршрута с промежуточными точками предполагается, что пользователь посетит каждую из этих точек на маршруте по очереди. Если промежуточная точка пропущена, маршрут может перестроиться:

* Если по данным GPS видно, что пользователь движется не в сторону нужной промежуточной точки, маршрут перестраивается относительно текущего положения пользователя так, чтобы он посетил пропущенную точку. Когда посещение точки будет зафиксировано, маршрут продолжится.
* Если момент посещения промежуточной точки не зафиксирован из-за слабого сигнала GPS, но позже положение пользователя зафиксировано на маршруте по направлению к следующей точке или около него, точка считается пройденной и маршрут продолжается.

## Параметры поиска маршрута {#route-parameters}

### Для автомобилей {#car-routing}

Чтобы построить маршрут для легкового автомобиля, укажите свойство `car` в [RouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteSearchOptions-class.html). Дополнительно вы можете настроить параметры построения маршрута [CarRouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/CarRouteSearchOptions-class.html):

* `avoidTollRoads`: избегать ли платных дорог (по умолчанию `false`).
* `avoidUnpavedRoads`: избегать ли грунтовых дорог (по умолчанию `false`).
* `avoidFerries`: избегать ли паромных переправ (по умолчанию `false`).
* `avoidLockedRoads`: избегать ли закрытых для проезда дорог (по умолчанию `true`).
* `routeSearchType`: способ учёта данных о пробках при построении маршрута. Вы можете строить маршрут с учётом реальных (`jam`) или статистических (`statistic`) данных о пробках или построить кратчайший по расстоянию маршрут, даже если он не является оптимальным по времени (`shortest`).
* `excludedAreas`: список областей (не более 25), которые следует избегать при построении маршрута. Подробнее см. в разделе [Исключение областей](#excluded-areas) ниже.

```dart
import 'package:dgis_mobile_sdk_full/dgis.dart' as sdk;

final carOptions = sdk.CarRouteSearchOptions(
    avoidTollRoads: true,
    avoidUnpavedRoads: true,
    avoidFerries: true,
    avoidLockedRoads: true,
    routeSearchType: sdk.RouteSearchType.jam,
    excludedAreas: []
  );

final routeSearchOptions = sdk.RouteSearchOptions.car(carOptions);
```

Передайте готовые параметры `routeSearchOptions` в вызов функции `findRoute()` при [построении маршрута](#building-route).

### Для такси {#taxi-routing}

Чтобы построить маршрут для такси, укажите свойство `taxi` в [RouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteSearchOptions-class.html).

Дополнительно вы можете настроить параметры построения маршрута [TaxiRouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/TaxiRouteSearchOptions-class.html). Для поиска маршрута для такси нужно сначала задать [параметры поиска обычных автомобильных маршрутов](#car-routing), а затем переиспользовать их в параметре `car`:

```dart
import 'package:dgis_mobile_sdk_full/dgis.dart' as sdk;

final taxiOptions = sdk.TaxiRouteSearchOptions(
    car: carOptions
  );

final routeSearchOptions = sdk.RouteSearchOptions.taxi(taxiOptions);
```

Передайте готовые параметры `routeSearchOptions` в вызов функции `findRoute()` при [построении маршрута](#building-route).

### Для грузового транспорта {#truck-routing}

Чтобы построить маршрут для грузового транспорта, укажите свойство `truck` в [RouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteSearchOptions-class.html). Дополнительно вы можете настроить параметры построения маршрута [TruckRouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/TruckRouteSearchOptions-class.html):

* `car`: базовые параметры поиска любых автомобильных маршрутов: см. [параметры поиска маршрута для автомобилей](#car-routing).
* `truckLength`: длина грузовика в миллиметрах.
* `truckHeight`: высота грузовика в миллиметрах.
* `truckWidth`: ширина грузовика в миллиметрах.
* `actualMass`: фактическая масса грузовика в килограммах.
* `maxPermittedMass`: разрешённая максимальная масса грузовика в килограммах.
* `axleLoad`: нагрузка на ось в килограммах.
* `dangerousCargo`: перевозится ли опасный груз (по умолчанию `false`).
* `explosiveCargo`: перевозятся ли взрывчатые вещества в грузе (по умолчанию `false`).
* `passIds`: список идентификаторов пропусков, которые разрешают движение грузового транспорта в пропускных зонах ([TruckPassZonePassId](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/TruckPassZonePassId-class.html)).
* `fallbackOnCar`: переключаться ли на поиск маршрута для легкового автомобиля, если по заданным параметрам невозможно найти грузовой маршрут (по умолчанию `false`).

```dart
import 'package:dgis_mobile_sdk_full/dgis.dart' as sdk;

final truckOptions = sdk.TruckRouteSearchOptions(
    car: carOptions,
    truckLength: 12000,                 // 12 метров
    truckHeight: 4200,                  // 4,2 метра
    truckWidth: 2500,                   // 2,5 метра
    actualMass: 18000,                  // 18 тонн
    maxPermittedMass: 20000,            // 20 тонн
    axleLoad: 8000,                     // 8 тонн
    dangerousCargo: true,
    explosiveCargo: false,
    passIds: <sdk.TruckPassZonePassId>{},
    fallbackOnCar: true
  );

final routeSearchOptions = sdk.RouteSearchOptions.truck(truckOptions);
```

Передайте готовые параметры `routeSearchOptions` в вызов функции `findRoute()` при [построении маршрута](#building-route).

:::note[Построение на онлайн-данных]

Для построения маршрутов для грузового транспорта используются только онлайн-данные. В офлайн-режиме маршруты не строятся. 

:::

### Для общественного транспорта {#public-transport-routing}

Чтобы построить маршрут для проезда на общественном транспорте, укажите свойство `publicTransport` в [RouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteSearchOptions-class.html). Дополнительно вы можете настроить параметры построения маршрута [PublicTransportRouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/PublicTransportRouteSearchOptions-class.html):

* `startTime`: время старта маршрута в UTC. Если не указано, используется текущее время.
* `useSchedule`: учитывать ли расписание движения общественного транспорта.
* `transportTypes`: список типов общественного транспорта, которые могут быть использованы для проезда по маршруту. Если список пустой, маршрут будет строиться для всех поддерживаемых типов транспорта. Полный список типов см. в [PublicTransportType](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/PublicTransportType.html).

```dart
import 'package:dgis_mobile_sdk_full/dgis.dart' as sdk;

final publicTransportOptions = sdk.PublicTransportRouteSearchOptions(
  startTime: DateTime.now().toUtc(),
  useSchedule: true,
  transportTypes: sdk.PublicTransportTypeEnumSet({
    sdk.PublicTransportType.bus,             // Автобус
    sdk.PublicTransportType.trolleybus,      // Троллейбус
    sdk.PublicTransportType.tram,            // Трамвай
    sdk.PublicTransportType.metro,           // Метро
    sdk.PublicTransportType.suburbanTrain,   // Пригородные поезда
  });
);

final routeSearchOptions = sdk.RouteSearchOptions.publicTransport(publicTransportOptions);
```

Передайте готовые параметры `routeSearchOptions` в вызов функции `findRoute()` при [построении маршрута](#building-route).

### Для велосипедов {#bicycle-routing}

Чтобы построить маршрут для велосипеда, укажите свойство `bicycle` в [RouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteSearchOptions-class.html). Дополнительно вы можете настроить параметры построения маршрута [BicycleRouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/BicycleRouteSearchOptions/BicycleRouteSearchOptions.html):

* `avoidCarRoads`: избегать ли автомобильных дорог (по умолчанию `false`).
* `avoidStairways`: избегать ли лестниц (по умолчанию `false`).
* `avoidUnderpassesAndOverpasses`: избегать ли подземных и надземных переходов (по умолчанию `false`).
* `excludedAreas`: список областей (не более 25), которые следует избегать при построении маршрута. Подробнее см. в разделе [Исключение областей](#excluded-areas) ниже.

```dart
import 'package:dgis_mobile_sdk_full/dgis.dart' as sdk;

final bicycleOptions = sdk.BicycleRouteSearchOptions(
  avoidCarRoads: true,
  avoidStairways: true,
  avoidUnderpassesAndOverpasses: true,
  excludedAreas: []
);

final routeSearchOptions = sdk.RouteSearchOptions.bicycle(bicycleOptions);
```

Передайте готовые параметры `routeSearchOptions` в вызов функции `findRoute()` при [построении маршрута](#building-route).

### Для самокатов {#scooter-routing}

Чтобы построить маршрут для самоката, укажите свойство `scooter` в [RouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteSearchOptions-class.html). Дополнительно вы можете настроить параметры построения маршрута [ScooterRouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/ScooterRouteSearchOptions-class.html):

* `avoidCarRoads`: избегать ли автомобильных дорог (по умолчанию `true`).
* `avoidStairways`: избегать ли лестниц (по умолчанию `true`).
* `avoidUnderpassesAndOverpasses`: избегать ли подземных и надземных переходов (по умолчанию `true`).
* `excludedAreas`: список областей (не более 25), которые следует избегать при построении маршрута. Подробнее см. в разделе [Исключение областей](#excluded-areas) ниже.

```dart
import 'package:dgis_mobile_sdk_full/dgis.dart' as sdk;

final scooterOptions = sdk.ScooterRouteSearchOptions(
  avoidCarRoads: true,
  avoidStairways: true,
  avoidUnderpassesAndOverpasses: true,
  excludedAreas: []
);

final routeSearchOptions = sdk.RouteSearchOptions.scooter(scooterOptions);
```

Передайте готовые параметры `routeSearchOptions` в вызов функции `findRoute()` при [построении маршрута](#building-route).

### Для пешеходов {#pedestrian-routing}

Пешеходные маршруты могут включать в себя перемещения внутри зданий (например, вы можете построить маршрут до определённого магазина в торговом центре). Если одна из точек маршрута находится внутри здания, для неё необходимо указать дополнительные параметры `objectId` (идентификатор объекта, к которому нужно проложить маршрут) и `levelId` (идентификатор этажа здания, на котором находится объект).

```dart
import 'package:dgis_mobile_sdk_full/dgis.dart' as sdk;

final finishPoint = sdk.RouteSearchPoint(
  coordinates: GeoPoint(latitude: 55.752425, longitude: 37.613983),
  objectId: sdk.DgisObjectId(
      objectId: 67890,
      entranceId: 2
  ),
  levelId: sdk.LevelId(value: 3));
```

Подробнее о ведении по маршруту внутри зданий см. в разделе [Навигация](/ios/sdk/examples/navigation#indoor-navigation).

Чтобы построить пешеходный маршрут, укажите свойство `pedestrian` в [RouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteSearchOptions-class.html). Дополнительно вы можете настроить параметры построения маршрута [PedestrianRouteSearchOptions](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/PedestrianRouteSearchOptions-class.html):

* `avoidStairways`: избегать ли лестниц (по умолчанию `false`).
* `avoidUnderpassesAndOverpasses`: избегать ли подземных и надземных переходов (по умолчанию `false`).
* `useIndoor`: строить ли маршруты внутри зданий (по умолчанию `true`).
* `excludedAreas`: список областей (не более 25), которые следует избегать при построении маршрута. Подробнее см. в разделе [Исключение областей](#excluded-areas) ниже.

```dart
import 'package:dgis_mobile_sdk_full/dgis.dart' as sdk;

final pedestrianOptions = sdk.PedestrianRouteSearchOptions(
  avoidStairways: false,
  avoidUnderpassesAndOverpasses: false,
  useIndoor: false,
  excludedAreas: []
);

final routeSearchOptions = sdk.RouteSearchOptions.pedestrian(pedestrianOptions);
```

Передайте готовые параметры `routeSearchOptions` в вызов функции `findRoute()` при [построении маршрута](#building-route).

### Исключение областей {#excluded-areas}

Чтобы исключить определённую область при построении маршрута, создайте объект [ExcludedArea](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/ExcludedArea-class.html) со следующими параметрами:

* `type`: геометрическая форма исключаемой области. Список возможных значений см. в [ExcludedAreaType](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/ExcludedAreaType.html).
* `severity`: приоритет исключения области (насколько важно её избегать). Список возможных значений см. в [ExcludedAreaSeverity](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/ExcludedAreaSeverity.html).
* `extent`: размер исключаемой области (не более 25 км).
* `points`: координаты точек исключаемой области типа [GeoPoint](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/GeoPoint-class.html) (не более 500).

Пример исключаемой области в форме многоугольника:

```dart
import 'package:dgis_mobile_sdk_full/dgis.dart' as sdk;

// Вершины многоугольника
final points = [
  sdk.GeoPoint(latitude: 55.759909, longitude: 37.618806),
  sdk.GeoPoint(latitude: 55.752425, longitude: 37.613983),
  sdk.GeoPoint(latitude: 55.753567, longitude: 37.622995),
  sdk.GeoPoint(latitude: 55.760523, longitude: 37.625613)
];

final polygonExcludedArea = sdk.ExcludedArea(
  type: sdk.ExcludedAreaType.polygon,
  severity: sdk.ExcludedAreaSeverity.hard,
  extent: sdk.RouteDistance(500.0),
  points: points
);
```

## Готовый маршрут {#using-ready-route}

Вы можете предварительно создать маршрут с помощью [Routing API](/api/navigation/routing/overview) и загрузить его для использования в мобильном SDK. Сохранённый маршрут доступен для использования в течение 24 часов. Маршруты для общественного транспорта недоступны для сохранения.

1. Создайте маршрут с помощью Routing API, сохраните его и скопируйте идентификатор маршрута: см. [Сохранение маршрута](/api/navigation/routing/examples/special#save-route).
2. Вызовите метод [fetchSharedRoute()](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/TrafficRouter/fetchSharedRoute.html) и передайте идентификатор сохранённого маршрута.

    ```dart
    import 'package:dgis_mobile_sdk_full/dgis.dart' as sdk;

    final routeId = "ROUTE_ID";

    final trafficRouter = sdk.TrafficRouter(sdkContext);
    final sdk.SharedRouteData sharedRouteData = await trafficRouter.fetchSharedRoute(routeId).value;
    ```

    Вызов вернёт отложенный результат с объектом [SharedRouteData](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/SharedRouteData-class.html), который содержит набор маршрутов (`List<TrafficRoute>`), точки маршрута и параметры поиска.

3. Чтобы отобразить маршрут на карте, создайте объекты [RouteMapObject](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteMapObject-class.html) на основе полученных объектов `TrafficRoute` и добавьте их в источник данных [RouteMapObjectSource](https://pub.dev/documentation/dgis_mobile_sdk_full/latest/dgis_mobile_sdk/RouteMapObjectSource-class.html).

    ```dart
    import 'package:dgis_mobile_sdk_full/dgis.dart' as sdk;

    // Создаём источник данных
    final sdk.RouteMapObjectSource routeMapObjectSource = sdk.RouteMapObjectSource(sdkContext, RouteVisualizationType.NORMAL);
    map.addSource(routeMapObjectSource);

    // Добавляем полученный маршрут на карту
    int routeIdx = 0;
    bool isActive = true;
    sharedRouteData.routes.forEach((sdk.TrafficRoute route) {
      routeMapObjectSource.addObject(sdk.RouteMapObject(route, isActive, routeIdx));
      routeIdx += 1;
      isActive = false;
    });
    ```

## Офлайн-режим {#offline-mode}

По умолчанию построение маршрута работает в гибридном режиме: в первую очередь используются данные с серверов <Brand />. Если за определённое время не удаётся построить маршрут на онлайн-данных, используются предварительно загруженные данные, если они есть.

Чтобы загрузить данные для работы офлайн, получите соответствующие права в ключе доступа и скачайте необходимые территории. См. [подготовительные шаги к работе с офлайн-данными](/flutter/sdk/start#offline-mode).

Следующие функции доступны только в онлайн-режиме:

* Построение маршрута для [грузового транспорта](#truck-routing).
* [Использование готового маршрута](#using-ready-route), который был построен ранее с помощью Routing API.


