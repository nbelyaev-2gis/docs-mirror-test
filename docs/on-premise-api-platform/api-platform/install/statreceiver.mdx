---
sidebar: on-premise-api-platform
---
# Установка сервиса сбора статистики

Установка сервиса сбора статистики необязательна, если вы не планируете собирать и анализировать статистические данные по использованию API-ключей для работы с сервисами API-платформы.

:::note[Пароли и ключи приведены для примера]

При реальной установке используйте более сложные и надёжные пароли.

:::

## 1. Перед установкой {#before-installing}

1. По возможности познакомьтесь с:

   * [Документацией сервиса сбора статистики](/on-premise-api-platform/architecture/api-platform/statreceiver)
   * [Архитектурой программного комплекса <Brand />](/on-premise-api-platform/architecture/solution-architecture)

2. Убедитесь, что выполнены необходимые предварительные шаги:

   1. [Подготовка к установке](/on-premise-api-platform/preparation)
   2. [Установка сервиса лицензий](/on-premise-api-platform/core/install/license)
   3. [Установка сервиса API-ключей](/on-premise-api-platform/core/install/keys)

3. Соберите необходимые данные, заданные или полученные на предыдущих шагах:

   | Объект                                                 | Пример значения                            | Как получить значение                                                         |
   | ------------------------------------------------------ | ------------------------------------------ | ----------------------------------------------------------------------------- |
   | Endpoint реестра Docker для хранения образов сервисов             | `docker.registry.example.com`            | См. [Получение артефактов установки](/on-premise-api-platform/preparation#artifacts) |
   | Секрет Kubernetes для доступа к реестру Docker | `onpremise-registry-creds`                 | См. [Получение артефактов установки](/on-premise-api-platform/preparation#artifacts)        |

4. Убедитесь, что удовлетворены требования к ресурсам, приведённые в [Helm-чарте](https://github.com/2gis/on-premise-helm-charts/blob/master/charts/stat-receiver/values.yaml).

   Подробнее о том, как это сделать, см. в документе [Системные требования](/on-premise-api-platform/requirements).

   :::note[Используйте подходящий чарт]

   Содержание Helm-чарта, описанное в данном разделе, актуально для последней версии API-платформы (см. [Релизы API-платформы](/on-premise-api-platform/api-platform/releases)). Чтобы изучить параметры для предыдущих версий, откройте [values.yaml в GitHub](https://github.com/2gis/on-premise-helm-charts/blob/master/charts/stat-receiver/values.yaml) и в списке тегов слева выберите тег `Platform-<версия>`.

   :::

5. Определите доменное имя:

    * для сервиса Stat Receiver — например, `stat-receiver.example.com`;
    * для сервиса Stat API (если вы планируете его устанавливать) — например, `stat-api.example.com`.

## 2. Установите сервис сбора статистики {#install-services}

### Установите сервис Stat Receiver

1. Создайте конфигурационный файл для Helm. Подробное описание доступных параметров см. [здесь](https://github.com/2gis/on-premise-helm-charts/blob/master/charts/stat-receiver/README.md).

   Пример файла уже заполнен всеми необходимыми данными, собранными на предыдущих этапах.

    <details>
    <summary><code>values-stat-receiver.yaml</code></summary>

   ```yaml
   dgctlDockerRegistry: docker.registry.example.com

   imagePullSecrets: [onpremise-registry-creds]

   kafka:
     servers: 'keys-kafka.storage.example.local:9092'
     securityProtocol: PLAINTEXT
     truststore:
       enabled: false
     keystore:
       enabled: false
     sasl:
       enabled: true
       secretName: 'stat-receiver-kafka-creds'
       jaasLoginModule: 'org.apache.kafka.common.security.scram.ScramLoginModule'
       username: ''
       password: ''

   initializeTopics:
     enabled: true
   topicsPrefix: 'stat_master_'
   ```

    </details>

   Где:

   * `dgctlDockerRegistry`: endpoint вашего реестра Docker, в котором находятся образы сервисов программного комплекса <Brand /> в формате `HOST:PORT`.
   * `imagePullSecrets`: [Kubernetes Secrets](https://kubernetes.io/docs/concepts/configuration/secret/#using-imagepullsecrets) для доступа к реестру Docker, в котором находятся образы сервисов программного комплекса <Brand />.
   * `kafka`: настройки Apache Kafka.

      * `servers`: адрес и порт кластера Apache Kafka, который был настроен на этапе [Установки сервиса API-ключей](/on-premise-api-platform/core/install/keys#configure-kafka).
      * `securityProtocol`: протокол безопасности для подключения к брокеру.
      * `truststore.enabled`: включите, чтобы использовать TLS для подключения к Kafka.
      * `keystore.enabled`: включите, чтобы использовать клиентскую аутентификацию TLS для подключения к Kafka.
      * `sasl`: настройка SASL для подключения к Kafka.

         * `enabled`: включите, если в сервисе API-ключей также включена аутентификация в Kafka.
         * `secretName`: имя секрета Kubernetes, в котором хранятся учётные данные для подключения к Kafka. Имя должно быть уникальным в пространстве имён кластера, который используется для установки.
         * `jaasLoginModule`: строка конфигурации JAAS для SASL-подключения.
         * `username`: имя пользователя для подключения к Kafka. Требуется, если в поле `secretName` пустое значение.
         * `password`: пароль для подключения к Kafka. Требуется, если в поле `secretName` пустое значение.

   * `initializeTopics.enabled`: включите, чтобы сервис сбора статистики создал необходимые топики в Kafka при первом запуске.
   * `topicsPrefix`: префикс для имен топиков, которые будут созданы в Kafka. См. имя топика, которое вы задали в параметре `kafka.stats.topic` в [конфигурационном файле сервиса API-ключей](/on-premise-api-platform/core/install/keys#install-keys): префикс — начальная часть имени топика до `type.401` (фиксированная часть имени).

2. Установите сервис с помощью Helm, используя подготовленный конфигурационный файл `values-stat-receiver.yaml`:

   ```bash
   helm upgrade --install --version=VERSION --atomic --wait --timeout 7200s --values ./values-stat-receiver.yaml stat-receiver 2gis-on-premise/stat-receiver
   ```

   В параметре `--version` укажите нужную версию API-платформы. Список версий см. в разделе [Релизы API-платформы](/on-premise-api-platform/api-platform/releases).

   :::warning

   Версии сервисов внутри компонента должны быть одинаковыми и совпадать с версией компонента.

   :::

### Установите сервис Stat API (опционально) {#install-stat-api}

Сервис Stat API нужен для отображения статистики распределения запросов в личном кабинете.

1. Создайте конфигурационный файл для Helm. Подробное описание доступных параметров см. [здесь](https://github.com/2gis/on-premise-helm-charts/blob/master/charts/stat-api/README.md).

   Пример файла уже заполнен всеми необходимыми данными, собранными на предыдущих этапах.

    <details>
    <summary><code>values-stat-api.yaml</code></summary>

   ```yaml
   dgctlDockerRegistry: docker.registry.example.com

   imagePullSecrets: [onpremise-registry-creds]

   migrate:
     kafkaTableEngine:
       brokers: broker1:9092,broker2:9092
       topic: type.401
       group: ''

     clickhouse:
       clientName: stat-api-migrate

   clickhouse:
     servers: host1:port1,host2:port2
     cluster: ''
     database: ''
     username: clickhouse-user
     password: password
   ```
   </ details>

   Где:

   * `dgctlDockerRegistry`: endpoint вашего реестра Docker, в котором находятся образы сервисов программного комплекса <Brand /> в формате `HOST:PORT`.
   * `imagePullSecrets`: [Kubernetes Secrets](https://kubernetes.io/docs/concepts/configuration/secret/#using-imagepullsecrets) для доступа к реестру Docker, в котором находятся образы сервисов программного комплекса <Brand />.
   * `migrate`: настройки для модуля миграции данных из Apache Kafka в ClickHouse.

       * `kafkaTableEngine`: параметры подключения к Apache Kafka для чтения данных в процессе миграции.
           * `brokers`: список адресов Kafka-брокеров, разделённых запятой.
           * `topic`: имя топика Apache Kafka, в котором находятся данные от сервиса Stat Receiver.
           * `group`: имя потребительской группы.

       * `clickhouse.clientName`: имя клиента, которое будет использоваться при подключении к ClickHouse.

   * `clickhouse`: параметры подключения к ClickHouse.
       * `servers`: список адресов серверов ClickHouse, разделённых запятой.
       * `cluster`: имя кластера ClickHouse при выполнении распределённых запросов и миграций.
       * `database`: имя базы данных ClickHouse для подключения.
       * `username`: имя пользователя для аутентификации в ClickHouse.
       * `password`: пароль пользователя для аутентификации в ClickHouse.

2. Установите сервис с помощью Helm, используя подготовленный конфигурационный файл `values-stat-api.yaml`:

   ```bash
   helm upgrade --install --version=VERSION --atomic --wait --timeout 7200s --values ./values-stat-api.yaml stat-api 2gis-on-premise/stat-api
   ```

   В параметре `--version` укажите нужную версию API-платформы. Список версий см. в разделе [Релизы API-платформы](/on-premise-api-platform/api-platform/releases).

   :::warning

   Версии сервисов внутри компонента должны быть одинаковыми и совпадать с версией компонента.

   :::

## 3. Проверьте работоспособность установленных сервисов {#test}

Когда вы установите все необходимые сервисы API-платформы, вы сможете просматривать статистику их использования в [Менеджере Платформы](/on-premise-api-platform/api-platform/user-guide/platform-manager#viewing-statistics) и настраивать лимиты в [веб-интерфейсе сервиса API-ключей](/on-premise-api-platform/core/keys).

## Что дальше? {#whats-next}

* Узнайте, как [обновить сервис сбора статистики](/on-premise-api-platform/api-platform/update/statreceiver).

* Установите продукты программного комплекса <Brand />:

  * [API для работы с картами](/on-premise-api-platform/api-platform/install/maps),
  * [API для работы с поиском](/on-premise-api-platform/api-platform/install/search),
  * [API для работы с навигацией](/on-premise-api-platform/api-platform/install/navigation),
  * [<Brand /> Про](/on-premise-pro/install/installation),
  * [<Brand /> Ситискан](/on-premise-citylens/install/installation),
  * [мобильный SDK](/on-premise-api-platform/api-platform/install/mobilesdk),
  * [GIS-платформа](/on-premise-gis-platform/installation),
  * [прокси для API пробок](/on-premise-api-platform/api-platform/install/trafficproxy),
  * [Менеджер Платформы](/on-premise-api-platform/api-platform/install/platform).

* Изучите рекомендации по [обслуживанию системы](/on-premise-api-platform/api-platform/administering).
