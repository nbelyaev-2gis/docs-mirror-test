# Обзор навигационных API <Brand />

Используйте навигационные API <Brand /> для построения маршрутов и получения информации о длине маршрута и времени в пути.

[Routing API](/api/navigation/routing/overview) помогает **строить маршруты на карте для разных видов транспорта**: автомобилей, такси, велосипедов, самокатов, мотоциклов, общественного транспорта, а также пешеходов. Вы можете добавлять промежуточные точки, строить кратчайшие по времени или расстоянию маршруты, исключать определённые области, а также учитывать пробки, дорожные перекрытия, выделенные полосы, лестницы, препятствия и другие параметры.

Для каждого маршрута вы можете получить длину, время в пути и полную геометрию. Для некоторых способов передвижения также можно получить информацию о высотах и о специальных точках (например, начале платного участка дороги). Для общественного транспорта вы можете получить информацию о пересадках, остановках, платформах, длине пешеходной части маршрута и других параметрах.

Попробуйте построить маршрут с Routing API:

<HtmlExampleWithSrcCode iframeStyles={{ height: '500px' }}>
{`
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Routing API</title>
  <meta name="description" content="Построение маршрутов с помощью Routing API" />
  <style>
    html,
    body,
    #container {
      margin: 0;
      width: 100%;
      height: 100%;
      overflow: hidden;
    }

    .start-banner {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 300px;
      background: #F2F2F2;
      padding: 12px 16px;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      text-align: center;
      z-index: 1000;
    }

    .controls {
      position: absolute;
      top: 10px;
      left: 10px;
      width: 240px;
      border-color: #D7D7D7;
      background: #FFFFFF;
      border-radius: 6px;
      padding: 8px 8px;
      z-index: 10;
    }

    .select-group {
      margin-top: 2px;
      margin-bottom: 12px;
    }

    .select-group label {
      font-family: 'Arial';
      font-size: 14px;
      color: #626262;
      padding: 0px 24px 8px 0px;
    }

    .select-group select {
      margin-top: 4px;
      border-color: #D7D7D7;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      width: 100%;
      -webkit-appearance: none;
      appearance: none;
      padding: 8px 24px 8px 8px;
      background:
        url("/img/drop-down.svg") no-repeat right 12px center,
        #F2F2F2;
      background-size: 14px 14px;
      box-sizing: border-box;
    }

    .select:active {
      background-color: #F2F2F2;
      color: #262626;
    }

    .select:hover {
      background-color: #F2F2F2;
      color: #0041C3;
    }

    .select:disabled {
      background-color: #F2F2F2;
      color: #929292;
      cursor: not-allowed;
    }

    .controls-button-calculate, .controls-button-reset {
      border: none;
      border-radius: 6px;
      padding: 8px 8px;
      font-family: 'Arial';
      font-size: 16px;
      display: block;
      width: 100%;
      box-sizing: border-box;
    }

    .select-group + .controls-button-calculate {
      margin-top: 20px;
    }

    .controls-button-calculate + .controls-button-reset {
      margin-top: 8px;
    }

    .controls-button-calculate:active, .controls-button-calculate:enabled {
      background-color: #0041C3;
      color: #E5E5E5;
    }

    .controls-button-reset:active, .controls-button-reset:enabled {
      background-color: #F2F2F2;
      color: #262626;
    }

    .controls-button-calculate:hover {
      background-color: #0028A6;
      color: #F2F2F2;
    }

    .controls-button-reset:hover {
      background-color: #E5E5E5;
      color: #0041C3;
    }

    .controls-button-calculate:disabled, .controls-button-reset:disabled {
      background-color: #E5E5E5;
      color: #929292;
      cursor: not-allowed;
    }

    .instructions-container {
      position: absolute;
      top: 80px;
      right: 50px;
      background: #FFFFFF;
      padding: 12px 16px;
      border-radius: 6px;
      width: 250px;
      height: 350px;
      z-index: 10;
      overflow-y: auto;
    }

    .instructions-container h3 {
      margin: 0 0 10px 0;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
    }

    .instructions-container p {
      margin: 0 0 8px 0;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      background: #F2F2F2;
      padding: 12px 16px;
      border-radius: 6px;
    }

    .route-info-container {
      position: absolute;
      top: 10px;
      right: 50px;
      width: 250px;
      background: #FFFFFF;
      padding: 12px 16px;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      text-align: left;
      z-index: 10;
      white-space: pre-line;
    }
  </style>
</head>

<body>
  <div id="container"></div>
  <div class="controls">
    <div class="select-group">
      <label for="transportSelect">Транспорт</label>
      <select id="transportSelect" name="transport">
        <option value="driving" selected>Автомобиль</option>
        <option value="taxi">Такси</option>
        <option value="bicycle">Велосипед</option>
        <option value="scooter">Самокат</option>
        <option value="motorcycle">Мотоцикл</option>
        <option value="truck">Грузовик</option>
        <option value="walking">Пешком</option>
      </select>
    </div>
    <button id="calculateButton" class="controls-button-calculate" disabled>Рассчитать маршрут</button>
    <button id="resetButton" class="controls-button-reset">Сбросить точки</button>
    <div class="tooltip" id="tooltip" style="display:none;"></div>
  </div>
  <div id="startBanner" class="start-banner">
    Отметьте три точки на карте
  </div>
  <script src="__MAPGL_API_URL__"></script>
  <script>
    const reqUrl = "https://routing.api.2gis.com/routing/7.0.0/global?key=Your directions API access key";

    const map = new mapgl.Map('container', {
      center: [37.615655, 55.768005],
      zoom: 11.5,
      key: 'Your API access key',
    });

    const startBanner = document.getElementById('startBanner');
    map.on('click', () => {
      if (startBanner) {
        startBanner.style.display = 'none';
      }
    });

    const markers = [];
    let firstPoint = null;
    let secondPoint = null;
    let thirdPoint = null;
    let routeLine = null;
    let selecting = 'a';

    const resetButton = document.getElementById('resetButton');
    const transportSelect = document.getElementById('transportSelect');
    const calculateButton = document.getElementById('calculateButton');
    const transportControls = document.querySelector('.controls');
    let transport = transportSelect.value;
    const transportFilters = [
      { id: 'btnDriving', transport: 'driving', filters: ["dirt_road", "toll_road", "ferry"] },
      { id: 'btnTaxi', transport: 'taxi', filters: ["dirt_road", "toll_road", "ferry"] },
      { id: 'btnBicycle', transport: 'bicycle', filters: ["ban_car_road", "ban_stairway"] },
      { id: 'btnScooter', transport: 'scooter', filters: ["ban_car_road", "ban_stairway"] },
      { id: 'btnMotorcycle', transport: 'motorcycle', filters: ["dirt_road", "toll_road", "ferry"] },
      { id: 'btnTruck', transport: 'truck', filters: ["dirt_road", "toll_road"] },
      { id: 'btnWalking', transport: 'walking', filters: ["dirt_road", "ferry", "highway"] },
    ];

    function removeElements(selectors) {
      selectors.forEach(selector => {
        const element = document.querySelector(selector);
        if (element) {
          element.remove();
        }
      });
    }

    function clearRouteLine() {
      if (routeLine) {
        routeLine.destroy();
        routeLine = null;
      }
    }

    resetButton.addEventListener('click', () => {
      selecting = 'a';
      firstPoint = null;
      secondPoint = null;
      thirdPoint = null;
      clearRouteLine();
      markers.forEach(m => m.destroy());
      markers.length = 0;
      transportSelect.disabled = false;
      calculateButton.disabled = true;
      routeLine = null;
      removeElements(['.instructions-container', '.route-info-container']);
      if (startBanner) startBanner.style.display = 'block';
    });

    map.on('click', (event) => {
      if (!event?.lngLat) return;
      const [lng, lat] = event.lngLat;

      if (selecting !== 'end') {
        let iconUrl;
        if (selecting === 'a') {
          iconUrl = 'https://disk.2gis.com/styles/assets/icons/start-2ec1e40c6a9f2b81815b42a4932012e3d6eda12e44b8591415424847c0f35260.svg';
        } else if (selecting === 'b') {
          iconUrl = 'https://disk.2gis.com/styles/assets/icons/interim_bus-fd37296da07136cf6815877ce3e1632b91571a77431cd79dc2d05b1d87a736ef.svg';
        } else if (selecting === 'c') {
          iconUrl = 'https://disk.2gis.com/styles/assets/icons/finish-22966aee51020ebd9f1a6c0a4a5e4333cb5f8a18fbffeeee693b7e18e6e5df78.svg';
        }

        const marker = new mapgl.Marker(map, {
          coordinates: [lng, lat],
          icon: iconUrl,
          zIndex: 1000,
        });
        markers.push(marker);
      }

      if (selecting === 'a') {
        firstPoint = { lon: lng, lat: lat };
        selecting = 'b';
      } else if (selecting === 'b') {
        secondPoint = { lon: lng, lat: lat };
        selecting = 'c';
      } else if (selecting === 'c') {
        thirdPoint = { lon: lng, lat: lat };
        selecting = 'end';
      }

      if (firstPoint && secondPoint && thirdPoint) {
        transportSelect.disabled = false;
        calculateButton.disabled = false;
        if (startBanner) startBanner.style.display = 'none';
      }
    });

    transportSelect.addEventListener('change', () => {
      transport = transportSelect.value;
    });

    calculateButton.addEventListener('click', () => {
      if (!(firstPoint && secondPoint && thirdPoint)) return;
      const filters = transportFilters[transport] || [];
      fetchRoute([firstPoint, secondPoint, thirdPoint], transport, filters);
    });

    transportFilters.forEach(({ id, transport, filters }) => {
      const btn = document.getElementById(id);
      if (!btn) return;
      btn.addEventListener('click', () => {
        fetchRoute([firstPoint, secondPoint, thirdPoint], transport, filters);
      });
    });

    function fetchRoute(points, transport, filters) {
      const pointTypes = {
        stop: ["driving", "taxi", "bicycle", "scooter", "motorcycle", "truck"],
        walking: ["walking"],
      };

      const startPointType = pointTypes.stop.includes(transport) ? "stop" : "walking";
      const endPointType = pointTypes.stop.includes(transport) ? "stop" : "walking";

      const formattedPoints = [
        { lon: points[0].lon, lat: points[0].lat, type: startPointType, start: true },
        { lon: points[1].lon, lat: points[1].lat, type: "pref" },
        { lon: points[2].lon, lat: points[2].lat, type: endPointType },
      ];

      const requestBody = {
        points: formattedPoints,
        locale: "ru",
        transport,
        filters,
        output: "detailed",
      };

      if (transport === "walking") {
        requestBody.params = {
          pedestrian: {
            use_instructions: true
          }
        };
      }

      fetch(reqUrl, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify(requestBody),
      })
        .then((res) => {
          if (!res.ok) {
            throw new Error('HTTP error! Status: ' + res.status);
          }
          return res.json();
        })
        .then((parsed) => {
          if (parsed.result && parsed.result.length > 0) {
            const totalDuration = parsed.result[0].ui_total_duration || 0;
            const totalDistanceValue = parsed.result[0].ui_total_distance.value || 0;
            const totalDistanceUnit = parsed.result[0].ui_total_distance.unit || 0;

            showRouteInfo(totalDuration, totalDistanceValue, totalDistanceUnit);

            const maneuvers = parsed.result[0].maneuvers;
            const coordinates = maneuvers.flatMap((maneuver) => {
              if (
                maneuver.outcoming_path &&
                maneuver.outcoming_path.geometry &&
                maneuver.outcoming_path.geometry.length > 0
              ) {
                return maneuver.outcoming_path.geometry
                  .flatMap((geometry) => {
                    const selection = geometry.selection;
                    return selection
                      .replace("LINESTRING(", "")
                      .replace(")", "")
                      .split(",")
                      .map((point) => point.trim().split(" ").map(Number));
                  });
              }
              return [];
            });

            if (coordinates.length > 0) {
              renderRoute(coordinates);
              renderManeuvers(maneuvers);
            } else {
              console.error("No coordinates found in response");
            }
          } else {
            console.error("No route found in response");
          }
        });
    }

    function showRouteInfo(duration, distanceValue, distanceUnit) {
      const routeInfoContainer = document.querySelector('.route-info-container') || document.createElement('div');
      routeInfoContainer.className = 'route-info-container';
      routeInfoContainer.innerHTML = 'Время в пути: ' + duration + '<br />Длина маршрута: ' + distanceValue + ' ' + distanceUnit;
      if (!routeInfoContainer.parentNode) {
        document.body.appendChild(routeInfoContainer);
      }
    }

    function renderManeuvers(maneuvers) {
      removeElements(['.instructions-container']);

      const instructionsContainer = document.createElement('div');
      instructionsContainer.className = 'instructions-container';

      const title = document.createElement('h3');
      title.textContent = 'Инструкции для маршрута';
      instructionsContainer.appendChild(title);

      maneuvers.forEach((maneuver, index) => {
        const instruction = document.createElement('p');
        let comment = maneuver.comment || '—';
        const outcomingPathComment = maneuver.outcoming_path_comment || '';
        if (comment === 'start') comment = 'Старт';
        if (comment === 'finish') comment = 'Финиш';
        instruction.textContent = (index + 1) + '. ' + comment + (outcomingPathComment ? ' (' + outcomingPathComment + ')' : '');
        instructionsContainer.appendChild(instruction);
      });

      document.body.appendChild(instructionsContainer);

      resetButton.onclick = () => {
        clearRouteLine();
        removeElements(['.instructions-container', '.route-info-container']);
        resetButton.textContent = 'Сбросить точки';
      };
    }

    function renderRoute(coordinates) {
      if (routeLine) {
        routeLine.destroy();
      }
      routeLine = new mapgl.Polyline(map, {
        coordinates,
        width: 6,
        color: "#0041C3",
      });
    }
  </script>
</body>

</html>`}
</HtmlExampleWithSrcCode>

Для **вычисления расстояний и времени в пути между несколькими точками отправления и прибытия** используйте [Distance Matrix API](/api/navigation/distance-matrix/overview). API позволяет оптимизировать маршруты (например, для курьерской доставки) и решать задачи логистики.

Укажите точки на карте и способ передвижения, чтобы получить данные для каждой пары точек. Например, для трёх точек отправления и трёх точек прибытия API рассчитает матрицу с расстоянием и временем в пути для девяти вариантов маршрутов. Также вы можете использовать асинхронный режим работы для расчёта запросов с большим количеством точек.

Пример использования Distance Matrix API в синхронном режиме:

<HtmlExampleWithSrcCode iframeStyles={{ height: '500px' }}>
{`
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Distance Matrix API</title>
  <meta name="description" content="Вычисление расстояний и времени в пути с помощью Distance Matrix API" />
  <style>
    html,
    body,
    #container {
      margin: 0;
      width: 100%;
      height: 100%;
      overflow: hidden;
    }

    .start-banner {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 300px;
      background: #F2F2F2;
      padding: 12px 16px;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      text-align: center;
      z-index: 1000;
    }

    .controls {
      position: absolute;
      top: 10px;
      left: 10px;
      width: 240px;
      border-color: #D7D7D7;
      background: #FFFFFF;
      border-radius: 6px;
      padding: 8px 8px;
      z-index: 10;
    }

    .select-group {
      margin-top: 2px;
      margin-bottom: 12px;
    }

    .select-group label {
      font-family: 'Arial';
      font-size: 14px;
      color: #626262;
      padding: 0px 24px 8px 0px;
    }

    .select-group select {
      margin-top: 4px;
      border-color: #D7D7D7;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      width: 100%;
      -webkit-appearance: none;
      appearance: none;
      padding: 8px 24px 8px 8px;
      background:
        url("/img/drop-down.svg") no-repeat right 12px center,
        #F2F2F2;
      background-size: 14px 14px;
      box-sizing: border-box;
    }

    .select:active {
      background-color: #F2F2F2;
      color: #262626;
    }

    .select:hover {
      background-color: #F2F2F2;
      color: #0041C3;
    }

    .select:disabled {
      background-color: #F2F2F2;
      color: #929292;
      cursor: not-allowed;
    }

    .controls-button-calculate, .controls-button-reset {
      border: none;
      border-radius: 6px;
      padding: 8px 8px;
      font-family: 'Arial';
      font-size: 16px;
      display: block;
      width: 100%;
      box-sizing: border-box;
    }

    .select-group + .controls-button-calculate {
      margin-top: 20px;
    }

    .controls-button-calculate + .controls-button-reset {
      margin-top: 8px;
    }

    .controls-button-calculate:active, .controls-button-calculate:enabled {
      background-color: #0041C3;
      color: #E5E5E5;
    }

    .controls-button-reset:active, .controls-button-reset:enabled {
      background-color: #F2F2F2;
      color: #262626;
    }

    .controls-button-calculate:hover {
      background-color: #0028A6;
      color: #F2F2F2;
    }

    .controls-button-reset:hover {
      background-color: #E5E5E5;
      color: #0041C3;
    }

    .controls-button-calculate:disabled, .controls-button-reset:disabled {
      background-color: #E5E5E5;
      color: #929292;
      cursor: not-allowed;
    }

    .tooltip {
      position: fixed;
      background: #FFFFFF;
      padding: 12px 16px;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      text-align: left;
      z-index: 20;
      display: none;
      white-space: pre-line;
    }

    .matrix-table-container {
      position: absolute;
      bottom: 10px;
      left: 10px;
      background: #FFFFFF;
      padding: 8px 8px;
      border-radius: 6px;
      z-index: 10;
      max-width: 90%;
    }

    .matrix-table {
      border-collapse: collapse;
      width: 100%;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
    }

    .matrix-table th,
    .matrix-table td {
      border: 1px solid #D7D7D7;
      padding: 8px;
      text-align: left;
    }

    .matrix-table th {
      background: #F2F2F2;
      text-align: center;
    }
  </style>
</head>

<body>
  <div id="container"></div>
  <div class="controls">
    <div class="select-group">
      <label for="transportSelect">Транспорт</label>
      <select id="transportSelect" name="transport">
        <option value="driving" selected>Автомобиль</option>
        <option value="taxi">Такси</option>
        <option value="bicycle">Велосипед</option>
        <option value="scooter">Самокат</option>
        <option value="motorcycle">Мотоцикл</option>
        <option value="truck">Грузовик</option>
        <option value="walking">Пешком</option>
      </select>
    </div>
    <button id="calculateButton" class="controls-button-calculate" disabled>Рассчитать маршрут</button>
    <button id="resetButton" class="controls-button-reset">Сбросить точки</button>
    <div class="tooltip" id="tooltip" style="display:none;"></div>
  </div>
  <div id="startBanner" class="start-banner">
    Отметьте на карте по порядку три точки отправления и три точки назначения
  </div>
  <script src="__MAPGL_API_URL__"></script>
  <script>
    const reqUrl = "https://routing.api.2gis.com/get_dist_matrix?key=Your directions API access key&version=2.0";

    const map = new mapgl.Map('container', {
      center: [37.615655, 55.768005],
      zoom: 11.5,
      key: 'Your API access key',
    });

    const startBanner = document.getElementById('startBanner');
    map.on('click', () => {
      if (startBanner) {
        startBanner.style.display = 'none';
      }
    });

    const startMarkers = [];
    const finishMarkers = [];
    let points = [];
    let sources = [];
    let targets = [];
    let polylines = [];

    const resetButton = document.getElementById('resetButton');
    const transportSelect = document.getElementById('transportSelect');
    const calculateButton = document.getElementById('calculateButton');
    const tooltip = document.getElementById('tooltip');
    let transport = transportSelect.value;

    map.on('click', (event) => {
      if (!event?.lngLat) return;
      if (points.length >= 6) return;

      const [lng, lat] = event.lngLat;
      const markerOptions = {
        coordinates: [lng, lat],
        icon: points.length === 0
          ? 'https://disk.2gis.com/styles/assets/icons/line10-dfd34cc8bb307bf445f15d2281ac18c17297d291e1db2cf95b4160615af8a7b8.svg'
          : points.length === 1
            ? 'https://disk.2gis.com/styles/assets/icons/line11-ec6b476db20af83278c478b487d14d2007f01bde48d7981c2899c96b848011e0.svg'
            : points.length === 2
              ? 'https://disk.2gis.com/styles/assets/icons/line13-a613ec85e32012d5f49ad26c03b218ca3e60799e6f62cc493b384df6ba15512f.svg'
              : points.length === 3
                ? 'https://disk.2gis.com/styles/assets/icons/line15-314f60e2c5fce3b433a7a300e46427449489e6aad0a63cce4087b8a907529feb.svg'
                : points.length === 4
                  ? 'https://disk.2gis.com/styles/assets/icons/line5-832d1c20c47476b223b472327df7bd60e8bfe37a5177194c76690d681bfd7dcf.svg'
                  : 'https://disk.2gis.com/styles/assets/icons/line8-8603d8e18f2b318d029bca729fe262486f0bc3b897f21aa0725f1b834bcb0038.svg',
        size: [25, 25],
        zIndex: 1000,
      };

      const marker = new mapgl.Marker(map, markerOptions);
      points.push({ lat, lon: lng });

      if (points.length <= 3) {
        startMarkers.push(marker);
        sources.push(points.length - 1);
      } else {
        finishMarkers.push(marker);
        targets.push(points.length - 1);
      }

      if (startBanner) {
        startBanner.style.display = 'none';
      }

      if (points.length === 6) {
        transportSelect.disabled = false;
        calculateButton.disabled = false;

        document.querySelectorAll('.controls-button[data-transport]').forEach(button => {
          button.disabled = false;
        });
      }
    });

    document.querySelectorAll('.controls-button').forEach(button => {
      button.addEventListener('click', function () {
        document.querySelectorAll('.controls-button').forEach(b => b.classList.remove('active'));
        this.classList.add('active');
        transport = this.dataset.transport;

        if (points.length === 6) {
          getDistanceMatrix();
        }
      });
    });

    calculateButton.addEventListener('click', () => {
      if (points.length !== 6) return;
      transport = transportSelect.value;
      getDistanceMatrix();
    });

    resetButton.addEventListener('click', () => {
      if (polylines.length > 0) {
        polylines.forEach(polyline => polyline.destroy());
        polylines.length = 0;
      }

      transportSelect.disabled = false;
      calculateButton.disabled = true;
      transportSelect.value = 'driving';
      transport = transportSelect.value;

      startMarkers.forEach(marker => marker.destroy());
      finishMarkers.forEach(marker => marker.destroy());
      startMarkers.length = 0;
      finishMarkers.length = 0;

      points = [];
      sources = [];
      targets = [];

      const existingTable = document.getElementById('matrixTable');
      if (existingTable) {
        existingTable.remove();
      }

      document.querySelectorAll('.controls-button').forEach(button => {
        if (button !== resetButton) {
          button.disabled = true;
          button.classList.remove('active');
        }
      });

      if (startBanner) {
        startBanner.style.display = 'block';
      }
    });

    function displayMatrix(matrix) {
      const existingTable = document.getElementById('matrixTable');
      if (existingTable) {
        existingTable.remove();
      }

      const tableContainer = document.createElement('div');
      tableContainer.id = 'matrixTable';
      tableContainer.className = 'matrix-table-container';

      const table = document.createElement('table');
      table.className = 'matrix-table';

      const headerRow = document.createElement('tr');
      const emptyHeader = document.createElement('th');
      emptyHeader.textContent = '';
      headerRow.appendChild(emptyHeader);

      points.slice(0, 3).forEach((point, index) => {
        const headerCell = document.createElement('th');
        headerCell.textContent = String.fromCharCode(68 + index); // D, E, F
        headerRow.appendChild(headerCell);
      });

      table.appendChild(headerRow);

      matrix.forEach((row, rowIndex) => {
        const tableRow = document.createElement('tr');

        const rowHeader = document.createElement('th');
        rowHeader.textContent = String.fromCharCode(65 + rowIndex); // A, B, C
        tableRow.appendChild(rowHeader);

        row.forEach((cell, colIndex) => {
          const tableCell = document.createElement('td');
          tableCell.innerHTML = 'Длина маршрута: ' + (cell.distance / 1000).toFixed(2) + ' км<br />' + 'Время в пути: ' + Math.round(cell.duration / 60) + ' мин';

          tableCell.addEventListener('mouseover', () => {
            const source = points[sources[rowIndex]];
            const target = points[targets[colIndex]];

            if (source && target) {
              const polyline = new mapgl.Polyline(map, {
                coordinates: [
                  [source.lon, source.lat],
                  [target.lon, target.lat]
                ],
                width: 8,
                color: '#FFFFFF',
                dashLength: 10,
                gapLength: 10,
                interactive: false
              });

              tableCell.polyline = polyline;
            }
          });

          tableCell.addEventListener('mouseout', () => {
            if (tableCell.polyline) {
              tableCell.polyline.destroy();
              tableCell.polyline = null;
            }

            tooltip.style.display = 'none';
            map.off('mousemove');
          });

          tableRow.appendChild(tableCell);
        });

        table.appendChild(tableRow);
      });

      tableContainer.appendChild(table);
      document.body.appendChild(tableContainer);
    }

    function getDistanceMatrix() {
      polylines.forEach(l => l.destroy());
      polylines.length = 0;
      tooltip.style.display = 'none';
      const body = {
        points,
        sources,
        targets,
        transport
      };
      fetch(reqUrl, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(body)
      })
        .then(res => {
          if (!res.ok) {
            throw new Error('HTTP error! Status: ' + res.status);
          }
          return res.json();
        })
        .then(parsed => {
          if (parsed.routes && parsed.routes.length > 0) {
            drawRoutes(parsed.routes);

            const matrix = [];
            sources.forEach((sourceId, i) => {
              const row = [];
              targets.forEach((targetId, j) => {
                const route = parsed.routes.find(r => r.source_id === sourceId && r.target_id === targetId);
                if (route) {
                  row.push({
                    distance: route.distance,
                    duration: route.duration
                  });
                } else {
                  row.push({
                    distance: 0,
                    duration: 0
                  });
                }
              });
              matrix.push(row);
            });

            displayMatrix(matrix);
          }
        })
        .catch(err => {
          console.error(err);
        });
    }

    function drawRoutes(routes) {
      const colors = ["rgba(0, 65, 195, 0.5)", "rgba(28, 169, 30, 0.5)", "rgba(254, 185, 25, 0.5)"];
      const sourceColors = {};

      routes.forEach((route, idx) => {
        const source = points[route.source_id];
        const target = points[route.target_id];

        if (!sourceColors[route.source_id]) {
          sourceColors[route.source_id] = colors[Object.keys(sourceColors).length % colors.length];
        }

        const polyline = new mapgl.Polyline(map, {
          coordinates: [
            [source.lon, source.lat],
            [target.lon, target.lat]
          ],
          width: 5,
          color: sourceColors[route.source_id],
          interactive: true
        });

        polyline.on('mouseover', (event) => {
          tooltip.innerHTML = 'Длина маршрута: ' + (route.distance / 1000).toFixed(2) + ' км<br />' + 'Время в пути: ' + Math.round(route.duration / 60) + ' мин';
          tooltip.style.display = 'block';
          const offset = 10;
          tooltip.style.left = (event.point[0] + offset) + 'px';
          tooltip.style.top = (event.point[1] + offset) + 'px';
        });

        polyline.on('mouseout', () => {
          tooltip.style.display = 'none';
        });

        polylines.push(polyline);
      });
    }

  </script>
</body>

</html>`}
</HtmlExampleWithSrcCode>

[TSP API](/api/navigation/tsp/overview) решает задачу коммивояжёра и **находит оптимальные маршруты для обхода нескольких точек** курьерами. При необходимости API оптимизирует маршрут по времени или расстоянию и исключает курьеров, если задачу можно решить с меньшими затратами. Вы можете указать способ передвижения курьеров, временные окна работы точек и время работы курьеров, вместимость курьеров и другие параметры. API работает в асинхронном режиме.

Попробуйте построить маршрут с TSP API:

<HtmlExampleWithSrcCode iframeStyles={{ height: '500px' }}>
{`
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>TSP API</title>
  <meta name="description" content="Решение задачи коммивояжёра с помощью TSP API" />
  <style>
    html,
    body,
    #container {
      margin: 0;
      width: 100%;
      height: 100%;
      overflow: hidden;
    }

    .start-banner {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 300px;
      background: #F2F2F2;
      padding: 12px 16px;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      text-align: center;
      z-index: 1000;
    }

    .controls {
      position: absolute;
      top: 10px;
      left: 10px;
      width: 240px;
      border-color: #D7D7D7;
      background: #FFFFFF;
      border-radius: 6px;
      padding: 8px 8px;
      z-index: 10;
    }

    .select-group {
      margin-top: 2px;
      margin-bottom: 12px;
    }

    .select-group label {
      font-family: 'Arial';
      font-size: 14px;
      color: #626262;
      padding: 0px 24px 8px 0px;
    }

    .select-group select {
      margin-top: 4px;
      border-color: #D7D7D7;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      width: 100%;
      -webkit-appearance: none;
      appearance: none;
      padding: 8px 24px 8px 8px;
      background:
        url("/img/drop-down.svg") no-repeat right 12px center,
        #F2F2F2;
      background-size: 14px 14px;
      box-sizing: border-box;
    }

    .select:active {
      background-color: #F2F2F2;
      color: #262626;
    }

    .select:hover {
      background-color: #F2F2F2;
      color: #0041C3;
    }

    .select:disabled {
      background-color: #F2F2F2;
      color: #929292;
      cursor: not-allowed;
    }

    .controls-button-calculate, .controls-button-reset {
      border: none;
      border-radius: 6px;
      padding: 8px 8px;
      font-family: 'Arial';
      font-size: 16px;
      display: block;
      width: 100%;
      box-sizing: border-box;
    }

    .select-group + .controls-button-calculate {
      margin-top: 20px;
    }

    .controls-button-calculate + .controls-button-reset {
      margin-top: 8px;
    }

    .controls-button-calculate:active, .controls-button-calculate:enabled {
      background-color: #0041C3;
      color: #E5E5E5;
    }

    .controls-button-reset:active, .controls-button-reset:enabled {
      background-color: #F2F2F2;
      color: #262626;
    }

    .controls-button-calculate:hover {
      background-color: #0028A6;
      color: #F2F2F2;
    }

    .controls-button-reset:hover {
      background-color: #E5E5E5;
      color: #0041C3;
    }

    .controls-button-calculate:disabled, .controls-button-reset:disabled {
      background-color: #E5E5E5;
      color: #929292;
      cursor: not-allowed;
    }

    .loading-banner {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      color: #F2F2F2;
      justify-content: center;
      align-items: center;
      font-family: 'Arial';
      font-size: 20px;
      z-index: 1000;
      display: none;
    }

    .route-info-container {
      position: absolute;
      top: 10px;
      right: 50px;
      background: #FFFFFF;
      padding: 12px 16px;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      text-align: left;
      z-index: 10;
      white-space: pre-line;
    }

    .tooltip {
      position: fixed;
      background: #FFFFFF;
      padding: 12px 16px;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      text-align: left;
      z-index: 20;
      display: none;
      white-space: pre-line;
    }
  </style>
</head>

<body>
  <div id="container"></div>
  <div class="controls">
    <div class="select-group">
      <label for="transportSelect">Транспорт</label>
      <select id="transportSelect" name="transport">
        <option value="driving" selected>Автомобиль</option>
        <option value="walking">Пешком</option>
        <option value="bicycle">Велосипед</option>
        <option value="truck">Грузовик</option>
      </select>
    </div>
    <div class="select-group">
      <label for="courierCount">Курьеры</label>
      <select id="courierCount" name="courierCount">
        <option value="1">1 курьер</option>
        <option value="2">2 курьера</option>
      </select>
    </div>
    <button id="calculateButton" class="controls-button-calculate" disabled>Рассчитать маршрут</button>
    <button id="resetButton" class="controls-button-reset">Сбросить точки</button>
    <div class="tooltip" id="routeTooltip" style="display:none;"></div>
  </div>
  <div class="tooltip" id="tooltip"></div>
  <div class="loading-banner" id="loadingBanner">Построение маршрута...</div>
  <div id="startBanner" class="start-banner">
    Отметьте на карте по порядку одну точку склада и три точки доставки
  </div>
  <div class="route-info-container" id="routeInfoContainer" style="display: none;"></div>
  <script src="__MAPGL_API_URL__"></script>
  <script>
    const apiKey = 'Your directions API access key';
    const reqTaskUrl = 'https://routing.api.2gis.com/logistics/vrp/1.1.0/create?key=' + apiKey;
    const map = new mapgl.Map('container', {
      center: [37.615655, 55.768005],
      zoom: 11.5,
      key: 'Your API access key',
    });

    const startBanner = document.getElementById('startBanner');
    map.on('click', () => {
      if (startBanner) {
        startBanner.style.display = 'none';
      }
    });

    const resetButton = document.getElementById('resetButton');
    const transportSelect = document.getElementById('transportSelect');
    const courierCount = document.getElementById('courierCount');
    const calculateButton = document.getElementById('calculateButton');
    const tooltip = document.getElementById('tooltip');
    const loadingBanner = document.getElementById('loadingBanner');
    const routeInfoContainer = document.getElementById('routeInfoContainer');

    let warehouse = null;
    let deliveryPoints = [];
    let markers = [];
    let routeLines = [];

    resetButton.addEventListener('click', resetMap);

    map.on('click', (event) => {
      if (!event?.lngLat) return;
      const [lng, lat] = event.lngLat;

      if (!warehouse) {
        warehouse = { lon: lng, lat: lat };
        addMarker([lng, lat], 'https://disk.2gis.com/styles/assets/icons/poi_new_building-6a88ffa5acccc9b63228e14b105d21343fa5291eedfe2e0d1f48abd061e48982.svg');
      } else if (deliveryPoints.length < 3) {
        deliveryPoints.push({ lon: lng, lat: lat });
        addMarker([lng, lat], 'https://disk.2gis.com/styles/assets/icons/poi_mall-16265747925045068731b6f7e938269f4bfa52acce62204bef135c5f31b33c5b.svg');
      }

      if (warehouse && deliveryPoints.length === 3) {
        enableControls();
      } else {
        disableControls();
      }
    });

    calculateButton.addEventListener('click', () => {
      if (!warehouse || deliveryPoints.length < 3) {
        return;
      }
      showLoadingBanner();
      const transportType = transportSelect.value;
      const courierCountValue = parseInt(courierCount.value, 10);
      createTSPTask(transportType, courierCountValue);
    });

    function addMarker(coordinates, icon) {
      const markerOptions = {
        coordinates,
        icon,
        size: [30, 30],
        zIndex: 1000,
      };

      const marker = new mapgl.Marker(map, markerOptions);
      markers.push(marker);
    }

    function resetMap() {
      markers.forEach((marker) => marker.destroy());
      markers = [];
      routeLines.forEach((polyline) => polyline.destroy());
      routeLines = [];
      warehouse = null;
      deliveryPoints = [];
      disableControls();
      hideRouteInfo();

      const startBanner = document.getElementById('startBanner');
      if (startBanner) {
        startBanner.style.display = 'block';
      }
    }

    function enableControls() {
      transportSelect.disabled = false;
      courierCount.disabled = false;
      calculateButton.disabled = false;
    }

    function disableControls() {
      transportSelect.disabled = false;
      courierCount.disabled = false;
      calculateButton.disabled = true;
    }

    function showLoadingBanner() {
      loadingBanner.style.display = 'flex';
    }

    function hideLoadingBanner() {
      loadingBanner.style.display = 'none';
    }

    function showRouteInfo(duration, distance) {
      routeInfoContainer.style.display = 'block';
      routeInfoContainer.innerHTML =
        'Общее время в пути: ' + Math.round(duration / 60) + ' мин<br />' +
        'Общая длина маршрута: ' + (distance / 1000).toFixed(2) + ' км';
    }

    function hideRouteInfo() {
      routeInfoContainer.style.display = 'none';
      routeInfoContainer.textContent = '';
    }

    function getPollUrl(taskId) {
      return 'https://routing.api.2gis.com/logistics/vrp/1.1.0/status?task_id=' + taskId + '&key=' + apiKey;
    }

    function createTSPTask(transportType, courierCount) {
      const tspRequest = {
        waypoints: [
          {
            waypoint_id: 0,
            point: { lat: warehouse.lat, lon: warehouse.lon },
          },
          ...deliveryPoints.map((point, index) => {
            const waypoint = {
              waypoint_id: index + 1,
              point: { lat: point.lat, lon: point.lon },
              service_time: 600,
              delivery_value: 10,
            };

            if (waypoint.waypoint_id === 1) {
              waypoint.time_windows = [{ start: 32400, end: 48600 }];
            }

            return waypoint;
          }),
        ],
        agents: Array.from({ length: courierCount }, (_, i) => ({
          agent_id: i,
          start_waypoint_id: 0,
          work_time_window: { start: 28800, end: 64800 },
          capacity: 500,
        })),
        routing_options: { transport: transportType },
        start_time: "2025-09-17T08:00:00Z",
      };

      console.log("API request:", tspRequest);

      return fetch(reqTaskUrl, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify(tspRequest),
      })
        .then((res) => {
          if (!res.ok) {
            throw new Error('API error: ' + res.status + ' ' + res.statusText);
          }
          return res.json();
        })
        .then((data) => {
          console.log("API response:", data);
          const taskId = data.task_id;
          if (!taskId) {
            throw new Error("No task_id in response");
          }
          pollTaskStatus(taskId);
        })
        .catch((error) => {
          hideLoadingBanner();
        });
    }

    function pollTaskStatus(taskId) {
      const reqPollUrl = getPollUrl(taskId);

      const interval = setInterval(() => {
        fetch(reqPollUrl, {
          method: "GET",
          headers: {
            "Content-Type": "application/json",
          },
        })
          .then((res) => res.json())
          .then((data) => {
            if (data.status === 'Done' || data.status === 'Partial') {
              clearInterval(interval);
              fetchSolution(data.urls.url_vrp_solution);
            } else if (data.status === 'Fail') {
              clearInterval(interval);
              hideLoadingBanner();
            }
          })
          .catch((error) => {
            clearInterval(interval);
            hideLoadingBanner();
          });
      }, 5000);
    }

    function fetchSolution(solutionUrl) {

      fetch(solutionUrl)
        .then((res) => res.json())
        .then((data) => {
          renderRoutes(data.routes);
          showRouteInfo(data.summary_duration, data.summary_distance);
          hideLoadingBanner();
        })
        .catch((error) => {
          hideLoadingBanner();
        });
    }

    function renderRoutes(routes) {
      routeLines.forEach((polyline) => polyline.destroy());
      routeLines = [];

      const tooltip = document.getElementById('tooltip');
      const colors = ["rgba(0, 65, 195)", "rgba(28, 169, 30)"];
      let colorIndex = 0;

      routes.forEach((route) => {
        const coordinates = route.points.map((pointId) => {
          if (pointId === 0) {
            return [warehouse.lon, warehouse.lat];
          } else {
            const point = deliveryPoints[pointId - 1];
            return [point.lon, point.lat];
          }
        });

        const polyline = new mapgl.Polyline(map, {
          coordinates,
          color: colors[colorIndex % colors.length],
          width: 7,
        });

        polyline.on('mouseover', (event) => {
          tooltip.innerHTML = 'ID курьера: ' + route.agent_id + '<br />Время в пути: ' + Math.round(route.duration / 60) + ' мин<br />' + ' Длина маршрута: ' + (route.distance / 1000).toFixed(2) + ' км';
          tooltip.style.left = (event.point[0] + 10) + 'px';
          tooltip.style.top = (event.point[1] + 10) + 'px';
        });

        polyline.on('mouseout', () => {
          tooltip.style.display = 'none';
        });

        routeLines.push(polyline);
        colorIndex++;
      });

      markers.forEach((marker, index) => {
        const pointId = index === 0 ? 0 : index;
        const point = index === 0 ? warehouse : deliveryPoints[index - 1];

        marker.on('mouseover', (event) => {
          tooltip.innerHtml = 'ID точки: ' + pointId + '<br />Координаты: [' + point.lon.toFixed(5) + ', ' + point.lat.toFixed(5) + ']';
          tooltip.style.display = 'block';
          tooltip.style.left = (event.point[0] + 10) + 'px';
          tooltip.style.top = (event.point[1] + 10) + 'px';
        });

        marker.on('mouseout', () => {
          tooltip.style.display = 'none';
        });
      });
    }
  </script>
</body>

</html>`}
</HtmlExampleWithSrcCode>

[Isochrone API](/api/navigation/isochrone/overview) **строит на карте зоны доступности** — области, до границ которых можно добраться от заданной точки за определённое время или наоборот, от границ до точки. Вы можете строить зоны доступности для разных видов транспорта.

Пример использования Isochrone API:

<HtmlExampleWithSrcCode iframeStyles={{ height: '500px' }}>
{`
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Isochrone API</title>
  <meta name="description" content="Построение зон доступности с помощью Isochrone API" />
  <style>
    html,
    body,
    #container {
      margin: 0;
      width: 100%;
      height: 100%;
      overflow: hidden;
    }

    .start-banner {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 300px;
      background: #F2F2F2;
      padding: 12px 16px;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      text-align: center;
      z-index: 1000;
    }

    .controls {
      position: absolute;
      top: 10px;
      left: 10px;
      width: 240px;
      border-color: #D7D7D7;
      background: #FFFFFF;
      border-radius: 6px;
      padding: 8px 8px;
      z-index: 10;
    }

    .select-group {
      margin-top: 2px;
      margin-bottom: 12px;
    }

    .select-group label {
      font-family: 'Arial';
      font-size: 14px;
      color: #626262;
      padding: 0px 24px 8px 0px;
    }

    .select-group select {
      margin-top: 4px;
      border-color: #D7D7D7;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      display: block;
      width: 100%;
      -webkit-appearance: none;
      appearance: none;
      padding: 8px 24px 8px 8px;
      background:
        url("/img/drop-down.svg") no-repeat right 12px center,
        #F2F2F2;
      background-size: 14px 14px;
      box-sizing: border-box;
    }

    .select:active {
      background-color: #F2F2F2;
      color: #262626;
    }

    .select:hover {
      background-color: #F2F2F2;
      color: #0041C3;
    }

    .select:disabled {
      background-color: #F2F2F2;
      color: #929292;
      cursor: not-allowed;
    }

  </style>
</head>

<body>
  <div id="container"></div>
  <div class="controls">
    <div class="select-group">
      <label for="reverseSelect">Направление</label>
      <select id="reverseSelect" name="reverse">
        <option value="from" selected>От точки</option>
        <option value="to">К точке</option>
      </select>
    </div>
    <div class="select-group">
      <label for="transportSelect">Транспорт</label>
      <select id="transportSelect" name="transport">
        <option value="driving" selected>Автомобиль</option>
        <option value="walking">Пешком</option>
        <option value="bicycle">Велосипед</option>
        <option value="motorcycle">Мотоцикл</option>
        <option value="public_transport">Общественный транспорт</option>
      </select>
    </div>
    <div class="select-group" id="publicTransportGroup" style="display: none;">
      <label for="publicTransportTypesSelect">Вид общественного транспорта</label>
      <select id="publicTransportTypesSelect" name="publicTransportTypes">
        <option value="bus" selected>Автобус</option>
        <option value="metro">Метро</option>
        <option value="tram">Трамвай</option>
        <option value="trolleybus">Троллейбус</option>
      </select>
    </div>
    <div class="select-group">
      <label for="durationSelect">Время</label>
      <select id="durationSelect" name="duration">
        <option value="300">5 минут</option>
        <option value="600" selected>10 минут</option>
        <option value="900">15 минут</option>
        <option value="1200">20 минут</option>
      </select>
    </div>
  </div>
  <div id="startBanner" class="start-banner">
    Переставляйте точку на карте
  </div>
  <script src="__MAPGL_API_URL__"></script>
  <script>
    const reqUrl = "https://routing.api.2gis.com/isochrone/2.0.0?key=Your directions API access key";
    let start = {
      lat: 55.762590,
      lon: 37.668598,
    };

    const map = new mapgl.Map('container', {
      center: [start.lon, start.lat],
      zoom: 11.5,
      key: 'Your API access key',
    });

    const startBanner = document.getElementById('startBanner');
    map.on('click', () => {
      if (startBanner) {
        startBanner.style.display = 'none';
      }
    });

    const reverseSelectEl = document.getElementById('reverseSelect');
    const transportSelectEl = document.getElementById('transportSelect');
    const durationSelectEl = document.getElementById('durationSelect');
    const publicTransportGroupEl = document.getElementById('publicTransportGroup');
    const publicTransportTypesSelectEl = document.getElementById('publicTransportTypesSelect');

    let startMarker = new mapgl.Marker(map, {
      coordinates: [start.lon, start.lat],
      zIndex: 1000,
    });

    let polygonsOnMap = [];

    function renderGeometries(geometry, transport) {
      for (let polygon of polygonsOnMap) {
        polygon.destroy();
      }
      polygonsOnMap = [];

      if (transport === "public_transport") {
        let polygon_groups = [];

        const wkt_polygon_groups = geometry.replace("MULTIPOLYGON(((", "((").replace(")))", "))").split(")), ");
        for (let wkt_polygon_group_str of wkt_polygon_groups) {
          wkt_polygon_group_str = wkt_polygon_group_str.indexOf("))") == -1 ? wkt_polygon_group_str + "))" : wkt_polygon_group_str;
          const wkt_polygon_group = wkt_polygon_group_str.replace("((", "(").replace("))", ")").split("), ");

          let polygon_group = [];
          for (let wkt_polygon_str of wkt_polygon_group) {
            wkt_polygon_str = wkt_polygon_str.indexOf(")") == -1 ? wkt_polygon_str + ")" : wkt_polygon_str;
            let polygon = wkt_polygon_str.replace("(", "").replace(")", "").split(",").map((point) => point.trim().split(" ").map(Number));

            polygon_group.push(polygon);
          }

          polygon_groups.push(polygon_group);
        }

        for (let polygon_group of polygon_groups) {
          polygon = new mapgl.Polygon(map, {
            coordinates: polygon_group,
            strokeWidth: 3,
            strokeColor: '#B20709',
            color: 'rgba(28, 169, 30, 0.25)'
          });

          polygonsOnMap.push(polygon);
        }
      } else {
        const coordinates = geometry
          .replace("MULTIPOLYGON(((", "")
          .replace(")))", "")
          .split(",")
          .map((point) => point.trim().split(" ").map(Number));

        const polygon = new mapgl.Polygon(map, {
          coordinates: [coordinates],
          strokeWidth: 3,
          strokeColor: '#B20709',
          color: 'rgba(28, 169, 30, 0.25)'
        });
        polygonsOnMap.push(polygon);
      }
    }

    window.addEventListener("change", (evt) => {
      if (
        evt.target.name === "reverse" ||
        evt.target.name === "transport" ||
        evt.target.name === "duration" ||
        evt.target.name === "publicTransportTypes"
      ) {
        updateGeometries();
      }
    });

    transportSelectEl.addEventListener("change", () => {
      if (transportSelectEl.value === "public_transport") {
        publicTransportGroupEl.style.display = "block";
        reverseSelectEl.disabled = true;
      } else {
        publicTransportGroupEl.style.display = "none";
        reverseSelectEl.disabled = false;
      }
    });

    map.on('click', (event) => {
      if (!event?.lngLat) return;
      const [lon, lat] = event.lngLat;
      start = { lat, lon };
      startMarker.setCoordinates([lon, lat]);
      updateGeometries();

      if (startBanner) {
        startBanner.style.display = 'none';
      }

    });

    updateGeometries();

    function updateGeometries() {
      for (let polygon of polygonsOnMap) {
        polygon.destroy();
      }
      polygonsOnMap = [];

      const transport = transportSelectEl.value;
      const duration = Number(durationSelectEl.value);
      const body = {
        start,
        durations: [duration],
        transport,
        format: "wkt",
        start_time: new Date().toISOString()
      };

      if (transport === "public_transport") {
        body.public_transport_types = [publicTransportTypesSelectEl.value];
        delete body.reverse;
      } else {
        body.reverse = reverseSelectEl.value === "to";
      }

      fetch(reqUrl, {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify(body)
      })
        .then((res) => {
          if (!res.ok) {
            throw new Error('HTTP error! Status: ' + res.status);
          }
          return res.json();
        })
        .then((parsed) => {
          if (parsed.isochrones && parsed.isochrones.length > 0) {
            renderGeometries(parsed.isochrones[0].geometry, transport);
          } else {
            console.error("No isochrones found in response");
          }
        })
        .catch((err) => console.error("Error fetching isochrone data:", err));
    }
  </script>
</body>

</html>`}
</HtmlExampleWithSrcCode>

[Map Matching API](/api/navigation/map-matching/overview) позволяет **восстановить автомобильный маршрут по точкам**, зафиксированным транспортным средством. Запрос возвращает полную геометрию маршрута, включая его длину и время в пути.

<br />

[Route Planner API](/api/navigation/route-planner/overview) помогает **построить наиболее эффективный маршрут, проходящий через все дороги внутри полигона**. API можно использовать для сценариев, в которых необходимо проехать по определённым улицам: например, для организации работы уборочной техники.

<br />

[Radar API](/api/navigation/radar/overview) помогает **определить приблизительное местоположение устройства при отсутствии спутникового сигнала**, а также при нестабильной работе других систем геопозиционирования. Radar API определяет местоположение по точкам доступа Wi-Fi и сотовым вышкам.

<br />

Подробные примеры запросов и справочники параметров доступны в документации для каждого навигационного API.

## FAQ

### Как начать работать с API навигации?

Чтобы работать с API, нужно получить ключ доступа:

1. Зарегистрируйтесь в личном кабинете [Менеджер Платформы](https://platform.2gis.ru/ru/?utm_source=docs&utm_medium=referral&utm_campaign=api_navi_faq).
1. Создайте [демо-ключ](/platform-manager/subscription/keys#demo) или купите [подписку](/platform-manager/subscription/purchase) для доступа к API.
1. Изучите пример запросов и ответов в документации к нужному API.

### Как рассчитывается стоимость использования API?

Стоимость использования API навигации может рассчитываться по количеству запросов к API, количеству построенных маршрутов или количеству расчётов времени и расстояния между парами точек. Подробнее о способе расчёта см. в разделе **Тарифы** на странице каждого API.

### Можно ли использовать API навигации бесплатно?

Вы можете [оформить демо-ключ доступа](/platform-manager/subscription/keys#demo) для тестирования API навигации на один месяц.

### Как отслеживать фактическое использование API-ресурсов?

Вы можете отслеживать прогресс расходования лимитов по сервисам и статистику распределения запросов в личном кабинете [Менеджер Платформы](https://platform.2gis.ru/ru/?utm_source=docs&utm_medium=referral&utm_campaign=api_navi_faq). Подробнее см. в разделе [Статистика](/platform-manager/subscription/statistics).

### Как контролировать потребление API-ресурсов?

Чтобы избежать преждевременного исчерпания лимитов, вы можете ограничить использование API-ресурсов вашими приложениями. В личном кабинете [Менеджер Платформы](https://platform.2gis.ru/ru/?utm_source=docs&utm_medium=referral&utm_campaign=api_navi_faq) доступно несколько способов настройки ограничений:

- Установить лимиты на запросы к конкретным сервисам по всем API-ключам в подписке или по отдельным ключам. Превышение лимитов заблокирует использование продукта: запросы к API будут возвращать ошибку.
- Настроить отправку уведомлений при приближении к лимитам.

Подробнее см. в разделе [Управление ключами доступа](/platform-manager/subscription/managing-keys).

### Как получить информацию о пробках?

Routing API, Distance Matrix API, TSP API, Isochrone API и Route Planner API поддерживают учёт текущих или статистических данных о дорожной обстановке, чтобы наиболее точно рассчитать оптимальный маршрут и время в пути. Отдельно информация о пробках не предоставляется.

### Как быстро обновляются данные о дорожной обстановке?

Информация о дорожной обстановке (пробки и перекрытия) обновляется в <Brand /> раз в несколько минут и становится доступна в ваших проектах одновременно с продуктами <Brand />.

### Как отобразить маршрут на карте?

- Для веб-приложений: установите векторную карту с помощью MapGL JS API и отобразите маршрут с помощью плагина Directions. Подробнее см. в разделе [Построение маршрутов](/mapgl/map/tools/directions).
- Для мобильных приложений: инициализируйте мобильный SDK <Brand /> и используйте инструменты для создания карты и построения маршрутов. Подробнее см. в разделе **Навигация** в документации SDK для [iOS](/ios/sdk/examples/routing), [Android](/android/sdk/examples/routing) или [Flutter](/flutter/sdk/examples/routing).

### Как создать навигатор по построенному маршруту?

Используйте мобильный SDK <Brand /> для создания навигатора по построенному маршруту. SDK предоставляет инструменты для отслеживания местоположения пользователя, поиску альтернативных маршрутов в пути, голосовых подсказок и других функций навигатора. Подробнее см. в разделе **Навигация** в документации SDK для [iOS](/ios/sdk/examples/navigation), [Android](/android/sdk/examples/navigation) или [Flutter](/flutter/sdk/examples/navigation).

### Можно ли установить сервисы навигации в собственной инфраструктуре?

Да, вы можете установить API-платформу <Brand /> в закрытом контуре. Подробнее см. в разделе [On-Premise](/on-premise-api-platform/overview/summary).

### Нет ответа на мой вопрос

Воспользуйтесь поиском по документации или отправьте электронное письмо на [api@2gis.ru](mailto:api@2gis.ru).