---
sidebar: suggestAPI
---
# Обзор

Suggest API предназначен для формирования подсказок для поиска объектов. Для отображения поисковых подсказок пользователю достаточно начать вводить текст в поле поиска. Suggest API предложит возможные варианты объектов, подходящих под критерии поиска. Пользователю останется только выбрать из предложенных вариантов. Подсказки учитывают местоположение пользователя.

<Video src="/video/Suggest_ru.mp4" width="900px" />

Запросы осуществляются методом GET, все необходимые параметры передаются в строке запроса. Ответы формируются в формате JSON.

Базовые настройки Suggest API оптимизированы для ускорения ввода запроса и совместного использования с [Places API](/api/search/places/overview). Если запрос состоит из нескольких критериев, например, «кафе с Wi-Fi», то часть результатов будет популярными запросами или контейнерами.

Чтобы получить конечный объект справочника, передайте полученные запросы или контейнеры на вход Places API. Чтобы использовать Suggest API без Places API, укажите подходящий `suggest_type`: подробнее см. в разделе [Типы подсказок](#suggestion-types).

:::tip[Расширенные возможности]

Изучите также [возможности других API поиска](/api/search/overview) и дополните ваши сценарии работы с объектами на карте.

:::

## Начало работы {#getting-started}

1. Получите ключ доступа:

   1. Зарегистрируйтесь в личном кабинете Менеджер Платформы.

      <CtaButton href="https://platform.2gis.ru/ru/?utm_source=docs&utm_medium=referral&utm_campaign=api_suggest" target="_blank">
         В кабинет
      </CtaButton>

   2. Создайте [демо-ключ](/platform-manager/subscription/keys#demo) или купите [подписку](/platform-manager/subscription/purchase) для доступа к API. Подробнее о стоимости сервиса см. в разделе [Тарифы](#tariffs).

   :::note[Данные по запросу]
   
   Для получения некоторой информации об объектах требуется дополнительное согласование. Изучите список методов и полей для получения [дополнительной информации по запросу](#info-on-demand).

   :::

   Подробнее о работе с ключами и подписками см. в [документации личного кабинета](/platform-manager/overview).

2. Изучите [формат запроса](#request) и [формат ответа](#response).

3. Изучите [примеры запросов](/api/search/suggest/examples) к Suggest API и полный [справочник API](/api/search/suggest/reference/3.0/suggests).

   Если вы хотите создать поисковый виджет на карте, изучите готовый пример [интеграции поисковых API в веб-приложение](./tutorial).

### Дополнительная информация по запросу {#info-on-demand}

Получение некоторой информации об объектах доступно только по запросу и за дополнительную плату. Cвяжитесь с [отделом продаж <Brand />](https://dev.2gis.ru/api#form), чтобы приобрести доступ к полям ниже.

Поля указываются с помощью параметра `fields`:

* `items.contact_groups` — контакты компании;
* `items.floors` — количество этажей;
* `items.floor_plans` — планы этажей;
* `items.links.database_entrances.apartments_info` — информация о квартирах в доме;
* `items.employees_org_count` — численность сотрудников организации;
* `items.itin` — индивидуальный номер налогоплательщика;
* `items.trade_license` — лицензия филиала;
* `items.fias_code` — код ФИАС улиц и административных территорий;
* `items.address.components.fias_code` — код ФИАС зданий;
* `items.fns_code` — код ФНС административных территорий;
* `items.okato` — код ОКАТО улиц и административных территорий;
* `items.address.components.okato` — код ОКАТО зданий;
* `items.oktmo` — код ОКТМО улиц и административных территорий;
* `items.address.components.oktmo` — код ОКТМО зданий;
* `items.structure_info.material` — данные о материале здания;
* `items.structure_info.apartments_count` — данные о количестве квартир;
* `items.structure_info.porch_count` — данные о количестве подъездов;
* `items.structure_info.floor_type` — тип перекрытий в здании;
* `items.structure_info.gas_type` — тип газоснабжения здания;
* `items.structure_info.year_of_construction` — год постройки здания;
* `items.structure_info.elevators_count` — количество лифтов в здании;
* `items.structure_info.is_in_emergency_state` — факт признания дома аварийным;
* `items.structure_info.project_type` — серия или проект постройки здания;
* `items.structure_info.chs_name` — название объекта культурного наследия;
* `items.structure_info.chs_category` — категория объекта культурного наследия.

## Формат запроса {#request}

```http
https://catalog.api.2gis.com/3.0/suggests?q=каф&location=37.630866,55.752256&key=YOUR_KEY
```

В запросе используются следующие параметры:

* `q=каф` — поиск выполняется по запросу «каф» и допускает разные варианты продолжения запроса. Например: «кафе», «кафетерий», «кафедральный собор», «кафель» и т. д.
* `location=37.630866,55.752256` — координаты точки, в окрестности которой выполняется поиск.
* `key=YOUR_KEY` — ваш ключ API.

### Типы подсказок {#suggestion-types}

Параметр запроса `suggest_type` отвечает за результат подбора подсказок. По умолчанию используется `suggest_type=object`, и подсказки относятся к объектам справочника (категориям, компаниям, улицам, городам и т. д.).

Самые используемые типы подсказок:

* [подсказка объектов справочника](#suggest-type-object),
* [подсказка адреса](#suggest-type-address),
* [подсказка конечной точки маршрута](#suggest-type-route-endpoint).

Полный список типов подсказок см. в [справочнике API](/api/search/suggest/reference/3.0/suggests).

#### Подсказка объектов справочника {#suggest-type-object}

В подсказке отображаются только объекты справочника: дома, компании, остановки и другие. В ответ попадают как текстовые подсказки, которые подбираются без привязки к точке на карте (например, «кафе с Wi-Fi»), так и конкретные объекты. В результаты также попадает и дополнение текста пользовательского запроса.

Чтобы получить подсказки по объектам справочника, добавьте в запрос параметр `suggest_type=object`.

#### Подсказка адреса {#suggest-type-address}

Подсказка адреса предлагает по введённой части адреса возможные варианты для ввода его остальной части.

Чтобы получить подсказки с названиями улиц и номерами домов, добавьте в запрос параметр `suggest_type=address`.

Чтобы получить подсказки только с названиями улиц, добавьте в запрос параметр `suggest_type=street`.

#### Подсказка конечной точки маршрута {#suggest-type-route-endpoint}

В подсказке отображаются дома, компании, остановки и другие объекты справочника, привязанные к точке на карте. Они подбираются по частичному или полному совпадению текста в поле поиска.

Чтобы получить подсказки по введённому фрагменту конечной точки маршрута, добавьте в запрос параметр `suggest_type=route_endpoint`.

## Формат ответа {#response}

Ответ на запрос возвращается в формате JSON:

```json
{
    "meta": {
        "api_version": "3.0.428313",
        "code": 200,
        "issue_date": "20200513"
    },
    "result": {
        "items": [
            {
                "search_attributes": {
                    "handling_type": 2,
                    "suggest_parts": [
                        {
                            "is_suggested": false,
                            "text": "каф"
                        },
                        {
                            "is_suggested": true,
                            "text": "е с wifi"
                        }
                    ],
                    "suggested_text": "кафе с wifi"
                },
                "type": "user_query"
            }
        ],
        "total": 1
    }
}
```

Ответ содержит следующие параметры:

* `suggested_text` — текст подсказки.

   * Для текстовых подсказок — это текст полного запроса, который Suggest API предлагает на основе текста, введённого пользователем.
   * Для подсказок объектов из справочника — это один из вариантов названия объекта, по которому Suggest API нашёл его. Например, если пользователь ввёл «кремль», а результат — "Московский кремль", то в `suggested_text` будет "Кремль".

* `suggest_parts` — составной объект, который показывает, какая часть текста была введена пользователем (`"is_suggested": false`), а какая добавлена Suggest API (`"is_suggested": true`).

## Тарифы {#tariffs}

* Стоимость сервиса рассчитывается исходя из количества успешных запросов в месяц. Успешным запросом к API считается запрос, который возвращает HTTP-код 200 в поле `meta.code` в теле ответа, например:

    ```json
    {
        "meta": {
            "api_version": "3.0.17799",
            "code": 200,
            "issue_date": "20240524"
        },
        ...
    }
    ```

* Актуальную стоимость и лимиты см. в разделе [Тарифы](/platform-manager/subscription/pricing).

## Варианты размещения {#distribution}

* **Облако**: все актуальные методы Suggest API доступны через публичные endpoint-ы <Brand />.
* **On-Premise**: все актуальные методы Suggest API доступны при установке API-платформы <Brand /> в закрытом контуре. Подробнее см. в разделе [API-платформа для сервера](/on-premise-api-platform/overview/summary).

Методы, отмеченные как `deprecated`, устарели и не поддерживаются в обоих вариантах размещения.

## Помощь {#support}

* Если у вас возникли вопросы при работе с API, задайте их AI-ассистенту (в правом нижнем углу cайта), воспользуйтесь поиском по документации или отправьте электронное письмо на [api@2gis.ru](mailto:api@2gis.ru).
* Если вы хотите обсудить возможности API или его интеграцию с вашим продуктом, обратитесь к менеджеру.

   <CtaButton href="https://dev.2gis.ru/api/suggest-api" target="_blank" appearance="lightFill">
     Получить консультацию
   </CtaButton>
